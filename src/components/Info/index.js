import React from 'react';

export const Info = ({name}) => {
    return (
        <div>
            
            <div>{name} </div>
          {/*   <div>Likes {favorites.length}</div> */}
           
        </div>
    );
};
